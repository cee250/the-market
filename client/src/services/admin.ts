const API_URL = (import.meta.env.VITE_API_URL ?? '').replace(/\/$/, '');
export interface AdminStats { users: number; vendors: number; publishedProducts: number; orders: number; paidMarketplaceRevenue: number; activeInventoryItems: number }
async function request<T>(path: string, init: RequestInit = {}): Promise<T> { const response = await fetch(`${API_URL}/api${path}`, { ...init, credentials: 'include', headers: { 'Content-Type': 'application/json', ...(init.headers ?? {}) } }); if (!response.ok) { const body = await response.json().catch(() => null) as { message?: string | string[] } | null; const message = Array.isArray(body?.message) ? body.message.join(' ') : body?.message; throw new Error(message || 'Unable to load admin operations'); } return response.json() as Promise<T>; }
export const adminApi = { stats: () => request<AdminStats>('/admin/stats'), audit: () => request<{ items: { id: string; action: string; entity: string; actor_name?: string; created_at: string }[] }>('/admin/audit-logs'), updateAccount: (input: { name?: string; password?: string }) => request<{ updated: boolean; name: string; passwordChanged: boolean }>('/admin/account', { method: 'PATCH', body: JSON.stringify(input) }) };
