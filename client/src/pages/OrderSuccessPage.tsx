import { Link, useParams } from 'react-router-dom';
import { ArrowRight, Check, CreditCard, MapPin, PackageX } from 'lucide-react';
import { EmptyState } from '../components/ui/EmptyState';
import { getOrder, ordersApi } from '../services/orders';
import { formatPrice } from '../lib/utils';
import { useAsync } from '../hooks/useAsync';
import type { Order } from '../types';
interface ServerOrder { id: string; order_number?: string; created_at?: string; subtotal?: number; delivery_fee?: number; total: number; status?: string; customer_name?: string; phone?: string; email?: string; delivery_address?: string; payment_method?: string; order_items?: { id: string; product_name: string; quantity: number; unit_price: number }[]; vendorOrders?: { items?: { id: string; product_name: string; quantity: number; unit_price: number }[] }[]; }
const normalize = (o: ServerOrder): Order => { const items = o.order_items ?? o.vendorOrders?.flatMap((vendor) => vendor.items ?? []) ?? []; return { id: o.order_number ?? o.id, items: items.map((i) => ({ id: i.id, name: i.product_name, image: 'https://placehold.co/80x80?text=Market', price: i.unit_price, qty: i.quantity })), subtotal: o.subtotal ?? o.total, discount: 0, shipping: o.delivery_fee ?? 0, total: o.total, customer: { name: o.customer_name ?? '', phone: o.phone ?? '', email: o.email ?? '', address: o.delivery_address ?? '', city: '' }, paymentMethod: (o.payment_method as Order['paymentMethod']) ?? 'mtn-momo', createdAt: o.created_at ?? new Date().toISOString(), status: (o.status ?? 'PENDING_PAYMENT').replaceAll('_', ' ').toLowerCase() }; };
export function OrderSuccessPage() {
  const { orderId } = useParams(); const local = getOrder(orderId ?? ''); const request = useAsync(async () => { if (local) return local; const found = await ordersApi.order(orderId ?? '') as ServerOrder; return found ? normalize(found) : undefined; }, [orderId, local?.id]); const order = request.data;
  if (request.loading) return <div className="mx-auto max-w-3xl px-4 py-16 sm:px-6"><div className="h-64 animate-pulse rounded-2xl bg-slate-100" /></div>;
  if (!order) return <div className="mx-auto max-w-7xl px-4 py-16 sm:px-6"><EmptyState icon={PackageX} title="Order not found" text="We couldn't find that order. It may have been placed in a different browser." action={{ label: 'Back to home', to: '/' }} /></div>;
  const firstName = order.customer.name.split(' ')[0] || 'there'; const date = new Date(order.createdAt).toLocaleDateString('en-GB', { day: 'numeric', month: 'long', year: 'numeric', hour: '2-digit', minute: '2-digit' });
  return <div className="mx-auto max-w-3xl px-4 py-12 sm:px-6"><div className="text-center"><span className="mx-auto flex h-16 w-16 items-center justify-center rounded-full bg-emerald-100 text-emerald-600 animate-pop"><Check size={30} strokeWidth={2.5} /></span><h1 className="mt-5 text-2xl font-extrabold text-slate-900 sm:text-3xl">Order confirmed, {firstName}!</h1><p className="mx-auto mt-2 max-w-md text-sm text-slate-500">Thank you for shopping with Market. A confirmation was sent to <strong className="text-slate-700">{order.customer.email}</strong>.</p></div><div className="mt-8 overflow-hidden rounded-2xl border border-slate-100 bg-white"><div className="flex flex-wrap items-center justify-between gap-3 border-b border-slate-100 bg-slate-50/60 px-5 py-4"><div><p className="text-xs uppercase tracking-wide text-slate-400">Order number</p><p className="text-sm font-bold text-slate-900">{order.id}</p></div><div className="text-right"><p className="text-xs uppercase tracking-wide text-slate-400">Placed</p><p className="text-sm font-semibold text-slate-700">{date}</p></div><span className="rounded-full bg-emerald-50 px-3 py-1 text-xs font-bold text-emerald-700">{order.status}</span></div><div className="divide-y divide-slate-100">{order.items.map((item) => <div key={item.id} className="flex items-center gap-4 px-5 py-4"><img src={item.image} alt="" className="h-14 w-14 rounded-xl bg-slate-50 object-cover" /><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold text-slate-900">{item.name}</p><p className="text-xs text-slate-400">Qty {item.qty}</p></div><p className="text-sm font-semibold text-slate-900">{formatPrice(item.price * item.qty)}</p></div>)}</div><div className="space-y-1.5 border-t border-slate-100 bg-slate-50/40 px-5 py-4 text-sm"><div className="flex justify-between"><span className="text-slate-500">Total</span><span className="font-extrabold">{formatPrice(order.total)}</span></div></div><div className="grid gap-4 border-t border-slate-100 px-5 py-5 sm:grid-cols-2"><div><h3 className="flex items-center gap-1.5 text-xs font-bold uppercase tracking-wide text-slate-400"><MapPin size={12} /> Delivering to</h3><p className="mt-2 text-sm font-semibold text-slate-900">{order.customer.name}</p><p className="mt-0.5 text-sm text-slate-600">{order.customer.address}</p><p className="mt-0.5 text-sm text-slate-600">{order.customer.phone}</p></div><div><h3 className="flex items-center gap-1.5 text-xs font-bold uppercase tracking-wide text-slate-400"><CreditCard size={12} /> Paid via</h3><p className="mt-2 text-sm font-semibold capitalize text-slate-900">{order.paymentMethod.replace(/-/g, ' ')}</p></div></div></div><div className="mt-8 flex flex-col justify-center gap-3 sm:flex-row"><Link to="/shop" className="flex items-center justify-center gap-2 rounded-lg bg-emerald-600 px-8 py-3 text-sm font-semibold text-white transition hover:bg-emerald-700">Continue shopping <ArrowRight size={16} /></Link><Link to="/orders" className="rounded-lg border border-slate-300 px-8 py-3 text-center text-sm font-semibold text-slate-700 transition hover:bg-slate-50">View my orders</Link></div></div>;
}
